
const initialState = {};

export const sideBarReducer = (state: any = initialState, action: any): any => {
    switch (action.type) {

        default: {
            return state;
        }
    }
};
